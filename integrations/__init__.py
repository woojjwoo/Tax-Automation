# Giwa Restaurant — Integration Clients
#
# Each client connects to one external service.
# All require API credentials set via environment variables
# or in config.py (copied from config_template.py).
